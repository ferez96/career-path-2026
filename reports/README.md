# Reports (`derived-sanitized`)

Only **sanitized, non-PII** outputs belong here (public-safe or agent context).

| Folder | Contents |
|--------|----------|
| `benchmarks/` | Benchmark / JD analysis (fit, gap, recommendations) |
| `briefs/` | Company / org brief (non-PII) |
| `private/` | Local-only reports — gitignored (e.g. full opportunity tracking / next-steps reports with company names; sanitize before any public excerpt) |

Raw input stays in `data/raw/` (gitignored); do not commit raw files under `reports/`.
