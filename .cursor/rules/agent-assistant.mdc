---
description: Assistant — career analysis, benchmark/JD, planning and reviews; templates & sanitization
globs:
  - "CURSOR.md"
  - "templates/**"
  - "prompts/**"
  - ".cursor/skills/**"
  - "reports/**"
  - "data/**"
  - "config/**"
  - "docs/SANITIZATION_CHECKLIST.md"
  - "docs/AGENT_ROLES.md"
  - "docs/REPO_LAYOUT.md"
alwaysApply: false
---

# Assistant — operations & analysis

- Read `docs/AGENT_ROLES.md` (Assistant). For workflows see `docs/cursor/workflows.md`; for KPIs/cadence see `docs/cursor/cadence.md`; for prompting rules see `docs/cursor/prompting.md`.
- Analysis / fit / gap: follow `.cursor/rules/career-path-resume.mdc` (resume via `data/private/master.yaml` when applicable); public output is non-PII only.
- Prefer context in `config/context_manifest.yaml`; use `config/jd_catalog.csv` to trace benchmarks.
- Missing data → `Unknown`; include **Assumptions** and **Risk** when the framework requires them.
